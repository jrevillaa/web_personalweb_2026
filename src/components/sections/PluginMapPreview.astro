---
const { title, description, categories = [], ctaText, ctaLink } = Astro.props;
---

<section class="plugin-map-preview">
  <div class="container">
    <div class="content-wrapper">
      <h2 class="section-title">{title}</h2>
      <p class="section-desc">{description}</p>
      
      <div class="tags-cloud">
        {categories.map((cat) => (
          <span class="tag">{cat}</span>
        ))}
        <span class="tag more">...</span>
      </div>

      <div class="cta-wrapper">
        <a href={ctaLink} class="btn btn-secondary">{ctaText}</a>
      </div>
    </div>
  </div>
</section>

<style>
  .plugin-map-preview {
    padding: var(--space-12) 0;
    background-color: var(--color-text);
    color: var(--surface-lowest);
    text-align: center;
  }

  .content-wrapper {
    max-width: 800px;
    margin: 0 auto;
  }

  .section-title {
    font-size: 2rem;
    margin-bottom: var(--space-4);
    color: var(--surface-lowest);
  }

  .section-desc {
    font-size: 1.1rem;
    color: var(--surface-highest);
    margin-bottom: var(--space-8);
  }

  .tags-cloud {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-bottom: var(--space-8);
  }

  .tag {
    background: var(--bg-on-dark-mid);
    padding: var(--space-2) var(--space-4);
    border-radius: 100px;
    font-size: 0.9rem;
    border: 1px solid var(--bg-on-dark-border);
    transition: background 0.2s ease;
  }

  .tag:hover {
    background: var(--bg-on-dark-border);
  }
  
  .tag.more {
    background: transparent;
    border: 1px dashed var(--bg-on-dark-dashed);
  }

  .btn {
    display: inline-block;
    padding: var(--space-3) var(--space-6);
    border-radius: var(--radius-2);
    font-weight: 600;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    text-decoration: none;
  }

  .btn-secondary {
    background-color: var(--surface-lowest);
    color: var(--color-text);
  }
  
  .btn:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-dark-soft);
  }
</style>
